language: python
python:
    - "3.4"
# command to install dependencies
install:
    - "pip install ."
# command to run tests
script: python3 $(which nosetests) -s --with-coverage --exe --cover-package=UniversalArchiveInterface